pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "org.jetbrains.kotlin.android" version "1.9.0"
        id "com.android.library" version "8.6.0"
    }
}

rootProject.name = 'pdf_render'
